# 📖 Полная инструкция по запуску Telegram бота

**Дата создания:** 24 ноября 2025  
**Цель:** Запустить бота через месяц (или позже) без правок кода

---

## 🎯 Что делает бот

- Поддерживает **2 AI модели**: Yandex GPT и GigaChat
- Получает **актуальные новости** из RSS-лент (РИА, ТАСС, Интерфакс)
- Показывает **погоду** через OpenWeatherMap API
- Находит **местоположения** на картах через Nominatim
- Веб-поиск через DuckDuckGo (резервный вариант)
- Переключение между моделями через кнопки

---

## 📋 Что нужно подготовить (API ключи)

### 1. Telegram Bot Token

1. Откройте Telegram, найдите [@BotFather](https://t.me/BotFather)
2. Напишите `/newbot`
3. Придумайте имя и username для бота
4. Скопируйте токен вида `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2. Yandex GPT (опционально)

1. Зайдите в [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Создайте каталог (folder), скопируйте его ID
3. Перейдите в **Сервисные аккаунты** → Создать
4. Назначьте роль `ai.languageModels.user`
5. Создайте **API ключ** (не IAM токен!) — он бессрочный
6. Сохраните: `YANDEX_FOLDER_ID` и `YANDEX_API_KEY`

**Важно:** Используйте именно API ключ, а не IAM токен (токен истекает через 12 часов).

### 3. GigaChat (опционально)

1. Зарегистрируйтесь на [developers.sber.ru/gigachat](https://developers.sber.ru/gigachat)
2. Создайте проект, получите **Client ID** и **Client Secret**
3. Закодируйте в Base64: `echo -n "ClientID:ClientSecret" | base64`
4. Сохраните результат как `GIGA_KEY`

**Пример:**
```bash
# Если Client ID = abc123, Client Secret = xyz789
echo -n "abc123:xyz789" | base64
# Результат: YWJjMTIzOnh5ejc4OQ==
```

### 4. OpenWeather API (опционально)

1. Зарегистрируйтесь на [openweathermap.org](https://openweathermap.org)
2. Перейдите в **API keys** → Create
3. Скопируйте ключ (бесплатный план: 1000 запросов/день)

---

## ⚙️ Настройка проекта

### Шаг 1: Скачайте проект

```bash
cd "путь/к/вашей/папке"
# Файлы уже должны быть на месте
```

### Шаг 2: Создайте файл `.env`

Создайте файл `.env` в корне проекта:

```env
# === ОБЯЗАТЕЛЬНО ===
TELEGRAM_TOKEN=ваш_telegram_token_от_BotFather

# === Yandex GPT (опционально) ===
YANDEX_FOLDER_ID=ваш_folder_id
YANDEX_API_KEY=ваш_api_key

# === GigaChat (опционально) ===
GIGA_KEY=ваш_base64_credentials
GIGA_SCOPE=GIGACHAT_API_PERS

# === Погода (опционально) ===
OPENWEATHER_API_KEY=ваш_openweather_key
```

**Важно:**
- Без `TELEGRAM_TOKEN` бот не запустится
- Без `YANDEX_*` и `GIGA_*` — будет работать только одна модель
- Без `OPENWEATHER_API_KEY` — погода не будет работать (не критично)

### Шаг 3: Проверьте сертификаты GigaChat

Если используете GigaChat, убедитесь, что в папке `certs/` есть `.cer` файлы:

```bash
ls certs/
# Должны быть файлы: russian_trusted_root_ca.cer и т.п.
```

Если файлов нет — скачайте с [e-trust.gosuslugi.ru](https://e-trust.gosuslugi.ru/ca).

---

## 🚀 Способ 1: Запуск через Python (Windows/Linux)

### Установка зависимостей

```bash
# Windows PowerShell
pip install -r requirements.txt

# Linux/Mac
pip3 install -r requirements.txt
```

**Если будет ошибка с `playwright`:**
```bash
playwright install chromium
```
(Но playwright больше не используется, так что можно пропустить)

### Запуск бота

```bash
# Windows
python unified_bot.py

# Linux/Mac
python3 unified_bot.py
```

**Бот запущен!** Логи сохраняются в `logs/unified_bot.log`.

### Остановка

Нажмите `Ctrl+C` в терминале.

### Автозапуск (Windows)

Используйте `start_bot.bat`:
```bash
start_bot.bat
```

---

## 🐳 Способ 2: Запуск через Docker (рекомендуется)

### Установка Docker

- **Windows/Mac:** Скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux:**
  ```bash
  curl -fsSL https://get.docker.com -o get-docker.sh
  sudo sh get-docker.sh
  sudo apt install docker-compose
  ```

### Запуск бота

```bash
# Сборка и запуск одной командой
docker-compose up -d --build

# Просмотр логов в реальном времени
docker-compose logs -f

# Остановка
docker-compose down
```

**Преимущества Docker:**
- Изолированная среда (не засоряет систему)
- Автоперезапуск при падении
- Легко переносить на другой сервер
- Логи в `./logs` доступны с хоста

### Управление

```bash
# Перезапуск после изменений
docker-compose restart

# Просмотр статуса
docker-compose ps

# Остановка без удаления
docker-compose stop

# Запуск после остановки
docker-compose start
```

---

## ✅ Проверка работы

### 1. Проверка запуска

После запуска в логах должно быть:

```
✅ RSS новости доступны (РИА, ТАСС, Интерфакс)
✅ Yandex GPT инициализирован успешно
✅ GigaChat клиент создан успешно
🤖 Объединенный Telegram бот запущен!
```

### 2. Тестирование в Telegram

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Должны появиться кнопки выбора модели
4. Выберите модель (Yandex GPT или GigaChat)
5. Напишите: **"Что случилось в России сегодня?"**

**Ожидаемый результат:**
- Бот выдаст список свежих новостей с датами (24.11.2025 или позже)
- Укажет источники (РИА, ТАСС)

### 3. Тест других функций

```
"Какая погода в Москве?" — должна вернуть актуальную погоду
"Где находится Красная площадь?" — карты с координатами
"Расскажи анекдот" — обычный ответ AI
```

---

## 🔧 Решение проблем

### Проблема 1: Бот не отвечает

**Причина:** Неверный `TELEGRAM_TOKEN`

**Решение:**
1. Проверьте токен в `.env` (должен быть полным, без пробелов)
2. Перезапустите бота

### Проблема 2: "Yandex GPT недоступна"

**Причины:**
- Неверный `YANDEX_FOLDER_ID`
- Истёк IAM токен (если используете не API ключ)
- Нет прав у сервисного аккаунта

**Решение:**
1. Убедитесь, что используете **API ключ** (не IAM токен)
2. Проверьте роль `ai.languageModels.user` у сервисного аккаунта
3. Пересоздайте API ключ в Yandex Cloud

### Проблема 3: "GigaChat недоступна"

**Причины:**
- Неверная Base64 кодировка `GIGA_KEY`
- Нет сертификатов в `certs/`

**Решение:**
1. Перекодируйте `ClientID:ClientSecret` заново:
   ```bash
   echo -n "ваш_client_id:ваш_client_secret" | base64
   ```
2. Скачайте сертификаты с [e-trust.gosuslugi.ru](https://e-trust.gosuslugi.ru/ca) в папку `certs/`

### Проблема 4: Бот не выдаёт актуальные новости

**Причина:** RSS-ленты недоступны (редко)

**Решение:**
1. Проверьте интернет-соединение
2. Посмотрите логи: `grep "RSS" logs/unified_bot.log`
3. Если RSS недоступен — новости будут из старого DuckDuckGo (не критично)

### Проблема 5: Docker контейнер сразу останавливается

**Решение:**
```bash
# Посмотрите логи контейнера
docker-compose logs telegram-bot

# Проверьте .env файл
cat .env
```

### Проблема 6: "Conflict: terminated by other getUpdates"

**Причина:** Бот уже запущен в другом месте (другой терминал/сервер)

**Решение:**
```bash
# Остановите все процессы Python
Get-Process python | Stop-Process -Force

# Или в Linux
pkill -f unified_bot.py

# Затем запустите заново
```

---

## 📁 Структура файлов

```
проект/
├── unified_bot.py          # Основной код бота
├── multi_search.py         # Мультипоиск DuckDuckGo
├── rss_news.py            # RSS новости (РИА, ТАСС)
├── requirements.txt        # Зависимости Python
├── .env                   # API ключи (СОЗДАТЬ!)
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker запуск
├── .dockerignore          # Игнор для Docker
├── README.md              # Основная документация
├── README_DOCKER.md       # Docker инструкции
├── FULL_SETUP_GUIDE.md    # ← ВЫ ЗДЕСЬ
├── certs/                 # Сертификаты GigaChat
│   └── russian_trusted_root_ca.cer
├── logs/                  # Логи (создаётся автоматически)
│   ├── unified_bot.log
│   └── errors.log
└── backups/               # Резервные копии (опционально)
```

---

## 🔄 Обновление через месяц

### Если что-то перестало работать:

1. **Проверьте API ключи:**
   ```bash
   cat .env
   ```
   - IAM токены истекают (перейдите на API ключи)
   - Токен бота может быть отозван (пересоздайте через BotFather)

2. **Обновите зависимости:**
   ```bash
   pip install --upgrade -r requirements.txt
   ```

3. **Проверьте RSS-ленты:**
   ```bash
   python rss_news.py
   ```
   Если не работает — обновите URL в `rss_news.py` (RSS_FEEDS)

4. **Пересоберите Docker образ:**
   ```bash
   docker-compose down
   docker-compose up -d --build
   ```

---

## 📊 Мониторинг

### Просмотр логов

```bash
# Python запуск
tail -f logs/unified_bot.log

# Docker запуск
docker-compose logs -f
```

### Статистика бота

Отправьте `/status` в Telegram — покажет:
- Время работы
- Количество обработанных сообщений
- Запросы к каждой модели
- Ошибки

---

## 🎯 Быстрая проверка всего за 2 минуты

```bash
# 1. Проверьте .env
cat .env | grep TELEGRAM_TOKEN

# 2. Запустите бота
python unified_bot.py
# или
docker-compose up -d

# 3. В Telegram отправьте боту:
/start
# Выберите модель
# Напишите: "Что случилось в России сегодня?"

# 4. Если ответ с актуальными новостями (с датами) — всё работает ✅
```

---

## 📞 Контакты и помощь

Если что-то не работает:

1. **Проверьте логи:** `logs/unified_bot.log` и `logs/errors.log`
2. **Проверьте .env:** все ключи должны быть без пробелов, кавычек
3. **Перезапустите:** иногда помогает простой перезапуск

---

## ✨ Фишки бота

- **Погода работает без API ключа** — бот даст ссылки на Яндекс.Погоду
- **Новости всегда свежие** — RSS обновляется каждый запрос
- **Безопасность:** `.env` не коммитится в git (в `.gitignore`)
- **Автоперезапуск:** Docker перезапустит бот при падении

---

## 🎉 Готово!

После выполнения инструкции бот будет работать **без дополнительных правок** даже через месяц.

**Главное:**
1. Файл `.env` с актуальными ключами
2. Сертификаты GigaChat в `certs/` (если используете GigaChat)
3. Интернет-соединение

**Запуск одной командой:**
```bash
docker-compose up -d
```

**Или:**
```bash
python unified_bot.py
```

---

**Последнее обновление:** 24 ноября 2025  
**Версия:** 1.0  
**Статус:** Протестировано и работает ✅

